# Temporary Android build runner

This repository branch is being used only as a public GitHub Actions runner workaround for MCP_ANDROID_EXTENSIONS.

Build trigger: MCP Android remote APK compilation.
